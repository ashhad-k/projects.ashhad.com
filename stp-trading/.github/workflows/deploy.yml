name: Deploy to live site

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./
          server-dir: /public_html/stptrading.saphyte.io/
          exclude: |
            **/.git*
            **/.git*/**
            **/.vscode/**
            **/.cursor/**
            **/node_modules/**
            **/scripts/**
            **/docs/**
            **/*.psd
            **/*.fig
            package.json
            package-lock.json
            .gitignore
